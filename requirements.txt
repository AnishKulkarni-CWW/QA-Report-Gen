streamlit>=1.36
pandas>=2.2
numpy>=1.26
# plotly still powers the INTERACTIVE on-screen dashboard charts.
plotly>=6.1.1
# matplotlib renders the STATIC chart images embedded in the Excel/PDF exports.
# It replaces kaleido entirely: no headless Chrome/Chromium, no subprocess, no
# timeouts, and it bundles cleanly into a PyInstaller .exe with everything
# working fully offline. (kaleido 1.x needed a system Chrome; kaleido 0.2.1's
# bundled Chromium hangs on some machines — matplotlib avoids both problems.)
matplotlib>=3.7
# Pillow reads each rendered chart's real pixel dimensions so PDF/Excel
# placements can preserve the true aspect ratio instead of a guessed one.
# It's already a transitive dependency of reportlab/openpyxl, but pinning it
# directly here makes sure PyInstaller sees and bundles it for the .exe.
Pillow>=10.0
# fonttools reads each embedded CJK font's glyph coverage (which Unicode
# characters it actually has) so Korean/Japanese Daily Log comments render
# with a font that has the right glyphs instead of missing-glyph boxes. It's
# already a transitive dependency of matplotlib, but pinned here directly for
# the same PyInstaller-visibility reason as Pillow above.
fonttools>=4.40
openpyxl>=3.1
reportlab>=4.2
