streamlit>=1.36
pandas>=2.2
numpy>=1.26
# plotly still powers the INTERACTIVE on-screen dashboard charts.
plotly>=6.1.1
# matplotlib renders the STATIC chart images embedded in the Excel/PDF exports.
# It replaces kaleido entirely: no headless Chrome/Chromium, no subprocess, no
# timeouts, and it bundles cleanly into a PyInstaller .exe with everything
# working fully offline. (kaleido 1.x needed a system Chrome; kaleido 0.2.1's
# bundled Chromium hangs on some machines — matplotlib avoids both problems.)
matplotlib>=3.7
openpyxl>=3.1
reportlab>=4.2
